this is a continuation of:

https://github.com/bahamas10/bash-md5
https://github.com/bahamas10/c-md5

thought it would be fun to dive into the rabbit hole directly.

first modified (C version) to match (within 2-5%) `md5sum` from `coreutils` and OpenSSL.

then hand-rolled ASM optis based on gcc `-O3` analysis => asm translations.

===

#   ./gen_asm.py > md5.s
#   as md5.s -o md5.o && ld md5.o -o md5
#   ./test-md5
